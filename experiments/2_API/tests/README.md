# Your API tests go here

Your agent writes into this folder. Import `test` and `expect` from [`../fixtures.ts`](../fixtures.ts)
to get the `foodoraApi` request context, then run them from the experiment folder:

```bash
npx playwright test tests/
```
