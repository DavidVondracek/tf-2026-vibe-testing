node_modules/

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/
.playwright-cli/
.playwright-mcp/
*.webm
*.zip

# Generated by `npx playwright init-agents` / `init-skills` during the workshop.
# Committed copies live in each exhibit's solutions/ folder instead.
.claude/
.agents/
.github/agents/
.github/prompts/

# Secrets — the Vercel AI Gateway key and the Wopee API key never land in the repo
.env
.env.local
*.local

# Editor / OS
.DS_Store
.idea/
